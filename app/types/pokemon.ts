export type DataProps = {
  data?: any;
  onClick?: () => void;
};

export type TypeFilterProps = {
  data?: any;
  onClick?: () => void;
  selected?: boolean;
};

export type TypeBadgeProps = {
  type?: string;
};
